{
  "name": "quickstart",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/stevenbister/quickstart",
  "license": "MIT",
  "devDependencies": {
    "eslint-config-prettier": "^6.10.0",
    "eslint-plugin-prettier": "^3.1.2",
    "prettier": "^1.19.1"
  },
  "scripts": {
    // Change the parcel.js output so we can host on GH pages
    "dev": "parcel index.html --out-dir docs",
    "build": "parcel build index.html --out-dir docs --public-url ./"
  }
}
